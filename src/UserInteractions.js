// import { WORLD_MAP as worldMapArr } from "./Constants.jsx";

// function movePlayer(dx, dy) {
//   const newX = playerX + dx;
//   const newY = playerY + dy;

//   // check if the new position is within the bounds of the map and is not a wall
//   if (
//     newX >= 0 &&
//     newX < worldMapArr[0].length &&
//     newY >= 0 &&
//     newY < worldMapArr.length &&
//     worldMapArr[newY][newX] === 1
//   ) {
//     setPlayerX(newX);
//     setPlayerY(newY);
//   }
// }

// export { movePlayer };
